---
interface Props {
  options: string[];
  active: string;
}

const { options, active } = Astro.props;
---

<nav
  aria-label="Filtro de proyectos"
  class="flex items-center flex-wrap gap-2 mb-5 text-sm"
>
  <span
    class="capitalize font-medium text-neutral-300"
  >
    filtros:
  </span>

  {
    options.map((option) => {
      const isActive = active === option;
      return (
      <button
          aria-pressed={isActive}
          data-filter={option}
          class={[
            "px-3 py-1.5 rounded-sm text-xs cursor-pointer border transition-colors duration-200 backdrop-blur-sm",
            "border-neutral-700 hover:border-white/75 hover:opacity-90",
            isActive &&
              "border-white shadow-sm"
          ]
            .filter(Boolean)
            .join(" ")
          }
        >
          {option}
        </button>
      );
    })
  }
</nav>
