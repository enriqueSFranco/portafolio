---
import CopyButton from "./CopyButton.astro"
---

<div
  id="contact-modal"
  aria-modal="true"
  role="dialog"
  class="
    fixed inset-0 z-[1000] hidden
    items-center justify-center p-4
    bg-black/40 backdrop-blur-sm
    opacity-0
    transition-opacity duration-300
  "
>
  <!-- PANEL -->
  <div
    class="
      relative w-full max-w-lg
      rounded-2xl
      bg-[var(--wolt-surface)]
      dark:bg-[var(--wolt-surface-dark)]
      border border-[var(--wolt-border)] dark:border-[var(--wolt-border-dark)]
      shadow-lg shadow-black/20 dark:shadow-black/40
      p-6 pb-8
      transform scale-95 opacity-0
      transition-all duration-300 ease-out
    "
    id="contact-modal-panel"
  >
    <!-- CLOSE BUTTON -->
    <button
      id="close-modal-button"
      aria-label="Cerrar modal"
      class="
        absolute top-4 right-4
        text-[var(--wolt-text-secondary)]
        dark:text-[var(--wolt-text-secondary-dark)]
        hover:text-[var(--wolt-text-primary)]
        dark:hover:text-[var(--wolt-text-primary-dark)]
        text-xl font-light
        transition
      "
    >
      &times;
    </button>

    <h3 class="text-2xl font-semibold text-[var(--wolt-text-primary)] mb-4">
      Hablemos de tu proyecto
    </h3>

    <div class="space-y-4">
      <p class="text-[var(--wolt-text-secondary)] dark:text-[var(--wolt-text-secondary-dark)]">
        ¡Excelente! Estoy listo para escuchar sobre tus ideas.
      </p>

      <CopyButton
        text="enrique.sfranco04@gmail.com"
        label="Copiar email"
      />
    </div>
  </div>
</div>
