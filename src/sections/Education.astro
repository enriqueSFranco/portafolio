<section
  id="educacion"
  aria-labelledby="educacion"
  class="min-h-screen flex flex-col px-4 py-20 sm:px-6 md:px-12 prose prose-invert max-w-none"
>
  <!-- ENCABEZADO -->
  <header class="mb-12 space-y-3 reveal-on-scroll">
    <h2
      id="education-title"
      class="text-3xl font-semibold tracking-tight text-white capitalize"
    >
      Educación
    </h2>

    <p class="text-zinc-400 leading-relaxed font-light max-w-md">
      Mi formación académica me dio bases sólidas en desarrollo y pensamiento estructurado.
    </p>
  </header>

  <!-- GRID MOBILE-FIRST -->
  <div class="flex flex-col gap-10 lg:grid lg:grid-cols-3 lg:gap-10">

    <!-- TARJETA PRINCIPAL -->
    <article
      class="p-6 sm:p-8 rounded-3xl bg-white/5 backdrop-blur-xl
             border border-white/10
             shadow-[0_8px_40px_-4px_rgba(0,0,0,0.45)]
             transition-all duration-500
             hover:shadow-[0_12px_45px_-2px_rgba(0,0,0,0.55)]
             hover:bg-white/7 reveal-on-scroll
             lg:col-span-2"
    >
      <h3 class="text-xl sm:text-2xl font-semibold leading-tight mb-2">
        Instituto Politécnico Nacional
        <span class="block text-base font-normal text-zinc-400">
          Escuela Superior de Cómputo (ESCOM)
        </span>
      </h3>

      <p class="text-base sm:text-lg text-zinc-300 font-light mb-6">
        Ingeniería en Sistemas Computacionales
      </p>

      <!-- Periodo -->
      <div class="mb-8">
        <p class="font-medium text-sm sm:text-base text-zinc-400">Periodo</p>
        <p class="text-lg sm:text-xl font-medium tracking-tight">
          Enero 2017 — Enero 2023
        </p>
      </div>

      <div class="h-px w-full bg-white/10"></div>

      <!-- Lista breve -->
      <ul class="mt-6 space-y-3 text-zinc-300 text-sm sm:text-base">
        <li class="flex items-center gap-3">
          <span class="text-white text-xl">🎓</span>
          Bases en estructuras de datos, sistemas y desarrollo web.
        </li>
        <li class="flex items-center gap-3">
          <span class="text-white text-xl">💡</span>
          Proyectos individuales y trabajo colaborativo.
        </li>
        <li class="flex items-center gap-3">
          <span class="text-white text-xl">⚙️</span>
          Metodologías ágiles, documentación y buenas prácticas.
        </li>
      </ul>
    </article>

    <!-- TARJETA ADICIONAL -->
    <aside
      class="p-6 sm:p-8 rounded-3xl bg-white/5 backdrop-blur-xl border border-white/10
             shadow-[0_8px_40px_-4px_rgba(0,0,0,0.35)]
             transition-all duration-500
             hover:shadow-[0_10px_50px_-2px_rgba(0,0,0,0.5)]
             hover:bg-white/7 reveal-on-scroll"
    >
      <h4 class="text-xl font-semibold mb-6 text-white">
        Complementos
      </h4>

      <ul class="space-y-4 text-zinc-300 text-sm sm:text-base">
        <li><span class="text-white mr-2">📘</span> Cursos online (JS, Web Dev)</li>
        <li><span class="text-white mr-2">🌐</span> Prácticas de desarrollo responsivo</li>
        <li><span class="text-white mr-2">⚙️</span> Buenas prácticas con Git</li>
        <li><span class="text-white mr-2">🚀</span> Trabajo en proyectos reales</li>
      </ul>
    </aside>
  </div>
</section>


<style is:inline>
  .reveal-on-scroll {
    opacity: 0;
    transform: translateY(12px);
    transition:
      opacity 0.6s ease,
      transform 0.6s ease;
  }

  .reveal-on-scroll.is-visible {
    opacity: 1;
    transform: translateY(0px);
  }
</style>

<script is:inline>
  const elements = document.querySelectorAll('.reveal-on-scroll');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible')
        observer.unobserve(entry.target);
      }
    })
  }, {
    threshold: 0.2
  })
  elements.forEach(el => observer.observe(el));
</script>
